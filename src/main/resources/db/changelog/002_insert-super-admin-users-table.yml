databaseChangeLog:
  - changeSet:
      id: 002_insert-super-admin-users-table
      author: Adil Ismayilov
      changes:
        - sql:
            sql: |
              INSERT INTO users (email, password, role, enabled, agreed_to_terms, status)
              SELECT
                '${superAdminEmail}',
                '${superAdminPassword}',
                'SUPER_ADMIN',
                true,
                true,
                'ACTIVE'
              WHERE NOT EXISTS (
                SELECT 1 FROM users WHERE email = '${superAdminEmail}'
              )
